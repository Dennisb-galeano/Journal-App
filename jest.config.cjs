
module.exports = {
  testEnvironment: 'jest-environment-jsdom',
  setupFiles: ['./jest.setup.js'],
  transformIgnorePatterns: [], //especifica patrones de modulos para que sean ignorados por parte de JEST.
}